pipeline {
    agent {
        docker { image 'nginx' }
    }
    stages {
        stage('Build NNGINX') {
            steps {
                sh 'pwd'
                sh 'ls -al'
            }
        }
    }
}
