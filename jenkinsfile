pipeline {
    //agent any
    triggers {
        pollSCM '* * * * *'
      }
    agent {
        docker { image 'nginx' }
    }
    stages {
        stage('Deploy Nginx') {
            steps {
                 
        sh """  
                docker images  
                ls -al
                
          """    
            }
        }
    }
}
