pipeline {
    agent {
        docker { image 'nginx' }
    }
    stages {
        stage('Build NGINX') {
            steps {
                sh 'pwd'
                sh 'ls -al'
            }
        }
    }
}
