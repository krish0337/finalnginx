pipeline {
    agent {
        docker { image 'ubuntu' }
    }
    stages {
        stage('Deploy NGINX') {
            steps {
                sh 'pwd'
                sh 'ls -al'
            }
        }
    }
}
