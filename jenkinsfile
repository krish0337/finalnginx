pipeline {
    //agent any
    triggers {
        pollSCM '* * * * *'
      }
    agent {
        docker { image 'bitnami/kubectl' }
    }
    stages {
        stage('Deploy Nginx') {
            steps {
                sh 'pwd'
                sh 'ls -al'
            }
        }
    }
}
